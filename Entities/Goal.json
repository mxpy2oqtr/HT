{
  "name": "Goal",
  "type": "object",
  "properties": {
    "type": {
      "type": "string",
      "enum": [
        "steps",
        "calories_burn",
        "calories_intake",
        "water",
        "sleep",
        "weight",
        "exercise_minutes"
      ],
      "description": "Tipo de meta"
    },
    "target_value": {
      "type": "number",
      "description": "Valor objetivo"
    },
    "current_value": {
      "type": "number",
      "default": 0,
      "description": "Valor actual"
    },
    "unit": {
      "type": "string",
      "description": "Unidad de medida"
    },
    "frequency": {
      "type": "string",
      "enum": [
        "daily",
        "weekly",
        "monthly"
      ],
      "description": "Frecuencia de la meta"
    },
    "start_date": {
      "type": "string",
      "format": "date",
      "description": "Fecha de inicio"
    },
    "end_date": {
      "type": "string",
      "format": "date",
      "description": "Fecha l\u00edmite"
    },
    "is_active": {
      "type": "boolean",
      "default": true,
      "description": "Si la meta est\u00e1 activa"
    }
  },
  "required": [
    "type",
    "target_value",
    "frequency"
  ]
}